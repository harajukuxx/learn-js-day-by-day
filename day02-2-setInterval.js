/*
setInterval(() => {
  console.log("Tick...");
}, 1000);
*/

/*
-------------------------------
let count = 0;

// เก็บค่าไว้ในตัวแปร timer
const timer = setInterval(() => {
  count++;
  console.log("วินาทีที่:", count);

  if (count === 5) {
    clearInterval(timer); // สั่งหยุดเมื่อถึง 5 วินาที
    console.log("หยุดแล้ว!");
  }
}, 1000);

*/
/*
-------------------------------
ลองทำโจทย์ "เครื่องนับเลข" (Counter)

ลองเขียน Object ตามโครงนี้ดูครับ:
ให้มี property count เริ่มต้นที่ 0
ให้มี method ชื่อ start
ใน start ให้ใช้ setInterval แบบ Arrow Function
ทุกๆ 1 วินาที ให้ this.count เพิ่มขึ้น 1 และ console.log ค่าออกมา

*/

/* ตอบเอง
const counter = {
  count: 0,
  start: function() {

        setInterval(()=>{
            this.count++
            console.log(this.count)
            if(this.count === 5){clearInterval(start)}
        },1000)

  }
};
counter.start();

-------------

จุดที่ต้องแก้ไข (The Bug)
ตรงบรรทัด clearInterval(start) ครับ:
ปัญหา: start คือชื่อของ Method (ฟังก์ชัน) ไม่ใช่ "ชื่อของตัวจับเวลา" ครับ
วิธีแก้: เราต้องสร้างตัวแปรมารับค่าจาก setInterval (เหมือนเป็น ID ของงานนั้น) เพื่อเอาไปบอก clearInterval ว่าให้หยุดงานไหน
*/

//ที่ถูกต้อง

/*
const counter2 = {
  count: 0,
  start: function() {
    // 1. สร้างตัวแปรเก็บ ID ของ Interval ไว้ (ใช้ const หรือ let ก็ได้)
    const timerId = setInterval(() => {
      this.count++;
      console.log(this.count);

      if (this.count === 5) {
        // 2. สั่งหยุดโดยอ้างอิง ID ที่เราเก็บไว้
        clearInterval(timerId); 
        console.log("Done!");
      }
    }, 1000);
  }
};

counter2.start();

*/

/*
-----------------------
โจทย์: สร้าง Object ชื่อ bomb

ให้มี Property timeLeft เริ่มต้นที่ 5 (วินาที)
ให้มี Method ชื่อ ignite (เริ่มจุดชนวน)
ภายใน ignite ให้ใช้ setInterval ด้วย Arrow Function ทำงานทุกๆ 1 วินาที โดยให้:
ลดค่า this.timeLeft ลงทีละ 1
แสดงข้อความใน console ว่า "T-Minus: [เลขเวลาที่เหลือ]..."
เงื่อนไขการหยุด: ถ้า this.timeLeft เท่ากับ 0 ให้สั่ง clearInterval เพื่อหยุดตัวจับเวลา และแสดงข้อความว่า "BOOM! 💥"
*/


const bomb ={
    timeLeft: 5,
    ignite: function(){
        const countdown = setInterval(() => {
            this.timeLeft--
            console.log("T-Minus: "+ this.timeLeft)
            if(this.timeLeft === 0){
                console.log("BOOM! 💥")
                clearInterval(countdown)
            }
        }, 1000);
    }

}

bomb.ignite()